import React from 'react';

interface Props{
    parkingSlotSize:number,
    vehicleSize:number,
}

const UnparkVehicle = (props:Props) => {
    //update the properties of the parking slot
    //slotAvailable should be false
    //slotCustomer should be null
}